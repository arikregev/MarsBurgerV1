@model MarsBurgerV1.Models.Drink
@{
    ViewBag.Title = "Details";
}

<div>
    <hr />
    <h4>Drink Details</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayName("Preview")
        </dt>
        <dd>
            <img src="@Url.Content(Model.ImageUrl)" alt="IMAGES" height="250" width="250" />
        </dd>
    </dl>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(m=>m.Price)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Price)
        </dd>
    </dl>
</div>
<p>
    @*@Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |*@
    @Html.Partial("_Back2ListButtonPartial")
    <a type="button" class="btn btn-sm btn-primary" href="@Url.Action("Edit", new { id = Model.Id })">
        <span class="glyphicon glyphicon-pencil"></span>
        <span>Edit Drink</span>
        </a>
</p>
